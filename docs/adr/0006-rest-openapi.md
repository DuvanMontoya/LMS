# ADR 0006: REST and OpenAPI

**Status:** Accepted — 2026-07-28.

Expose `/api/v1` REST described by OpenAPI and generate the TypeScript client from it. This supports web and future mobile without duplicated transport types. GraphQL is rejected absent a demonstrated multi-client query problem.
